import { InputMaskDirective } from './input-mask-directive';

describe('InputMaskDirective', () => {
  it('should create an instance', () => {
    expect(new InputMaskDirective()).toBeTruthy();
  });
});
